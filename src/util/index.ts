import createTree from './createTree';
import mapPropName from './mapPropName';
import removeProp from './removeProp';

const bind = {
  createTree,
  mapPropName,
  removeProp,
};

export default bind;
export { createTree, mapPropName, removeProp };
